---
listing:
  # Show each project by its own landing page (index.qmd in subfolders)
  contents: /*/index.qmd
  type: table            # or "default", "table"
  fields: [title, description, categories]
---

The list of projects remains a work in progress.
